<Window x:Class="DADesktopUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DADesktopUI.Views"
        mc:Ignorable="d" FontSize="12"
        Title="ShellView" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top" FontSize="12">
            <MenuItem Header="_File">
            </MenuItem>
            <MenuItem x:Name="LoginScreen" Header="_Account" Visibility="{Binding IsAccountVissible,
                Converter={StaticResource BooleanToVisibilityConverter},
                FallbackValue=Collapsed}">
                <MenuItem x:Name="LogOut" Header="_Log Out" />
            </MenuItem>
        </Menu>
        <Grid>
            <ContentControl x:Name="ActiveItem" Margin="5" />
        </Grid>
    </DockPanel>
</Window>